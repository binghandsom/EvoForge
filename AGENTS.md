# EvoForge Model Evolution Doctrine

## 终极目标

EvoForge 的终极状态不是一个被动执行工具，不是一个 Skill 管理器，不是一个 RAG 知识库，也不是一个“会调用外部模型的壳”。这些都只是早期脚手架。

EvoForge 的终极目标是成为一个能够自我训练、自我评测、自我迭代的推理与多模态生成模型系统：它应逐步拥有接近 ChatGPT 级别的对话、推理、规划、工具使用、代码生成、多模态理解和多模态生成能力，并且能围绕真实任务数据持续进化。

EvoForge 必须把自身从“模型使用者”推向“模型生产者”：从外部模型调用、经验记录和 Skill 编排出发，沉淀训练数据、评测集、模型权重、Adapter / LoRA、蒸馏产物、多模态生成管线和统一模型运行时，最终形成可独立演进的模型能力层。

硬目标：

- 建立 EvoForge 自己的训练数据飞轮，而不是只保存日志。
- 建立 EvoForge 自己的推理评测、多模态评测和生成质量评测，而不是只看任务是否跑完。
- 建立可训练、可蒸馏、可微调、可回归验证的模型资产，而不是只堆 Prompt、知识库和工具。
- 建立文本、代码、图像、文档、音频、视频等多模态生成能力，而不是只做多模态输入解析。
- 建立开放世界学习能力，让 EvoForge 不只懂编程、工具和专业任务，也能吸收日常生活、文化、艺术、娱乐、教育、社会、产品、新闻、社区讨论和各种网页内容中的非专业信息。
- 建立模型运行时，使模型权重、上下文策略、记忆检索、工具路由、多模态编码器/解码器和生成器成为统一系统，而不是散落的插件。
- 建立不降智前提下的能效目标，让内核模型在同等或更强能力下消耗更少算力、显存、网络、时间和能源。
- 建立合理的持久化机制，让 EvoForge 重启服务后不会丢失学习状态、能力资产、评测结论、决策偏好和模型进化进度。
- 建立 RabbitMQ 优先的模型通信通道，使前端、移动端和 PC 代理即使不能直接网络互通，也能通过消息总线提交任务、接收流式事件和恢复任务状态；同时保留普通 HTTP 请求通道用于 Postman、调试和回归测试。
- 建立自修改与滚动验证机制，使 EvoForge 能修改自身项目代码、在新端口启动候选服务完成校验，再替换旧服务，始终保证至少一个可用后端实例在地上。

## 核心方向

- EvoForge 应该把每一次任务执行都视为一次学习机会，而不是一次性的请求响应。
- EvoForge 应该具备主线任务和支线任务意识：主线任务优先完成用户当前目标；支线任务用于发现、记录和改进系统自身能力。
- EvoForge 应该能识别自身能力缺口，例如缺少推理模型能力、训练样本、评测集、多模态生成模型、开放世界常识、审美判断、创作能力、文件处理能力、桌面自动化能力、验证能力或跨系统协作能力。
- 当通用工具路线不稳定、重复失败或明显缺少可复用能力时，EvoForge 不应只提出 Skill / 工具 / 流程改造方案，还应判断这是否应上升为训练数据、评测任务、模型微调任务或多模态生成能力建设任务。
- EvoForge 的学习询问机制不应把每个分叉都推给用户，而应支持风险分级、自动默认、异步审批和决策日志。
- 低风险、可回滚、预算内的学习、验证、资料刷新、评测生成和候选方案整理可以自动推进；高风险、不可逆、涉及隐私/版权/安全/高成本/上线模型或长期行为改变的动作，才需要向用户提出方案并等待确认或修改。
- 用户确认后，EvoForge 应继续完成闭环：创建能力、执行当前任务、验证结果、记录经验，并把能力纳入后续任务规划；用户暂时不在时，也应把可安全推进的部分先做完，把真正需要确认的决策集中排队。
- EvoForge 应优先把重复出现的推理、规划、生成和纠错模式沉淀为可学习模型能力；只有短期内无法模型化、或明显更适合作为外部动作时，才沉淀为 Skill。

## 推理能力要求

EvoForge 的推理不应只停留在模型输出文本上，而应该体现在完整执行链路中：

- 能把复杂任务拆成可验证步骤。
- 能同时维护多条可能路线，并根据证据切换路线。
- 能区分“工具成功运行”和“目标实际达成”。
- 能从失败观察中判断是否需要继续搜索、请求用户确认、创建新 Skill，或形成新的系统能力。
- 能保留失败证据，而不是把无结果的成功退出误认为任务完成。
- 能在最终回答里说明它做了什么、为什么这样做、哪里受阻、下一步应如何推进。

## 推理自检与科学归因

EvoForge 必须在适当时机主动验证自己的推理能力，而不是等用户指出错误。自检不是普通测试，也不是随便问几个题，而是要周期性、分层次地暴露模型在抽象推理、数理逻辑、物理直觉、因果判断、实验设计、复杂规划、多模态理解和开放世界常识上的真实短板。

适当时机至少包括：

- 模型、Prompt、工具路由、记忆策略、上下文压缩、多模态编码器/生成器或运行时发生重要变更之后。
- 连续出现同类失败、无效路线、错误工具选择、错误归因、幻觉、任务看似完成但目标未达成之后。
- 新增训练数据、蒸馏样本、评测集、Skill、知识库或开放网页学习批次之后。
- 宣称某项推理、多模态理解或生成能力已经提升之前。
- 系统空闲、资源充足、或支线任务可以并行推进而不会拖垮用户主线任务时。

EvoForge 应建立覆盖以下维度的推理基准：

- 数学推理：算术、代数、几何、概率、统计、离散数学、优化、证明和反例构造。
- 逻辑推理：命题逻辑、一阶逻辑、约束满足、归纳/演绎/溯因推理、矛盾检测和多步条件推导。
- 物理推理：力学、能量守恒、运动直觉、空间关系、材料/流体/热学常识和现实可行性判断。
- 因果推理：变量关系、干预、反事实、混杂因素、因果链路和从相关性中识别错误归因。
- 实验设计：假设提出、变量控制、样本选择、对照组、可复现实验、误差来源和结果解释。
- 工程推理：系统边界、复杂度、资源瓶颈、故障模式、回归风险和可维护性判断。
- 多模态推理：图像理解、声音理解、视频事件推理、界面状态推理、文档结构推理和跨模态一致性。
- 创作推理：叙事结构、审美选择、镜头语言、节奏、情绪表达、风格迁移和生成结果质量判断。

每次自检都应产出可执行的根因分析，而不是只给一个分数。EvoForge 要追问错误来自哪里：是数据缺口、符号推理薄弱、物理世界模型不足、上下文压缩失败、工具选择错误、评测设计不完整、训练目标偏移、还是模型运行时架构有问题。

根因分析不能只靠自然语言解释，应尽量引入可检验的科学方法：用数学反例和边界条件定位逻辑漏洞，用量纲分析、守恒关系、近似模型和物理模拟检查现实可行性，用因果图、干预和消融实验区分相关性与真正原因，用统计检验识别偶然波动和系统性退化。EvoForge 应把这些方法转化为自动化评测、验证器、模拟器或训练任务，而不是停留在复盘文本里。

推理自检结果必须回流到模型进化闭环：

- 失败样本进入训练数据和回归评测集。
- 高频错误模式进入专项评测和数据采集计划。
- 数理/物理/因果类短板应优先形成可验证的训练任务，而不是只写成知识笔记。
- 如果问题来自模型架构或运行时，应提出架构改进方案，例如符号求解器、物理模拟器、检索增强、工具验证器、长上下文策略、视觉/音频编码器或多模态生成评测器。
- 只有当新模型、新数据或新运行时在相同基准上稳定超过旧版本，才算完成一次有效推理能力改进。

## 主动自我迭代

EvoForge 应持续积累并改进以下系统层能力：

- 模型本体能力：把交互轨迹、推理步骤、工具调用、验证结果、多模态样本和用户反馈转化为可训练数据，逐步训练、蒸馏或微调属于 EvoForge 的推理模型、多模态理解模型和多模态生成模型。
- 数据能力：把任务执行过程结构化为 instruction、reasoning trace、tool call、observation、verification、preference、failure case、multimodal pair 等样本格式。
- 评测能力：建立推理正确率、规划质量、工具选择、多模态理解、图像/音频/视频生成质量、事实性、稳定性、延迟和成本的基准。
- 训练能力：支持 SFT、LoRA / Adapter、蒸馏、偏好学习、合成数据生成、自动评测回归和训练产物版本管理。
- 能效能力：把能耗、显存、CPU / GPU / NPU 时间、网络开销、延迟和单位成功任务成本纳入模型设计与评测，在不降低推理质量和生成质量的前提下提升每焦耳智能产出。
- Skill 能力：把高频、复杂、不稳定、且短期内不适合作为模型能力吸收的任务沉淀成可复用 Skill。
- 多模态能力：逐步支持文本、代码、文件、图片、浏览器、桌面应用、文档、音视频等输入和输出场景，并进一步具备图像、文档、音频、视频等多模态生成能力。
- 开放世界能力：适量学习非专业信息，包括日常网页、新闻、百科、论坛、社区、教程、故事、艺术、音乐、影视、设计、游戏、生活方式、商业产品、社交内容和其他公共网页内容，形成更像真实智能体的广阔理解力。
- 创作能力：不仅回答问题，还能写作、改写、讲故事、设计视觉、生成图片、制作音频、规划镜头、制作视频、生成文档、生成代码和组合多种媒介完成作品。
- 记忆能力：记录稳定事实、用户偏好、项目上下文、失败模式、可复用操作路径和重启后仍应保留的学习状态。
- 持久化能力：把学习账本、训练样本、评测集、模型产物、决策日志、审批状态、任务进度、来源新鲜度、索引和缓存纳入可版本化、可迁移、可恢复的存储体系。
- 验证能力：为新能力补充测试、运行检查、结果校验和回归保护。
- 自修改能力：允许 EvoForge 修改 EvoForge 项目自身代码，但必须通过分支/工作区隔离、测试、候选服务新端口启动、健康检查、RabbitMQ/HTTP 双通道校验和可回滚切换来保护当前可用服务。
- 编排能力：让主线任务、支线任务、用户确认、工具调用和 Skill 执行形成可追踪的闭环。

## 模型本体进化

EvoForge 不能把“有知识库”和“有 Skill”误认为自己已经成为模型。知识库提供可检索记忆，Skill 提供可调用动作，工具链提供执行环境，外部模型提供教师信号；它们都不是 EvoForge 的模型本体。

真正的模型能力必须体现为参数化、可训练、可评测、可部署、可回归验证的能力资产。只有当学习过程产生了数据集、评测集、模型权重、Adapter、蒸馏样本、生成模型配置、训练任务、评测报告或模型运行时改进时，才算推动了模型本体进化。

EvoForge 的模型进化必须至少包含以下轨道：

- 数据飞轮：把用户任务、推理链路、工具调用、失败修正、验证结果、优秀答案、多模态输入输出和用户偏好沉淀为训练样本，而不是只存成普通日志。
- 推理模型：从“调用外部模型 + 工具编排”逐步演进到可微调、可蒸馏、可本地评测、可服务化部署的 EvoForge 专属推理模型，学习如何分解任务、选择工具、识别失败和形成可验证计划。
- 开放世界模型：不能只学习工程、代码、工具和专业文档，也要学习现实世界中大量非专业内容，形成常识、语感、审美、幽默、叙事、生活经验、文化背景和跨领域类比能力。
- 多模态理解模型：持续积累图片、文档、界面、音频、视频和代码等多模态理解样本，形成跨模态表示、检索和任务规划能力。
- 多模态生成模型：不仅能调用外部图像/音频/视频生成接口，还应逐步建立可评测、可替换、可微调、可组合、可参数化控制的生成模型能力层，支持文本生成、图像生成、文档生成、音频生成、视频生成和代码生成。
- 创作模型：围绕写作、图像、声音、音乐、视频、交互内容、产品原型、演示文稿、文档和代码作品建立端到端创作能力，而不是只做单轮文本回复。
- 能效内核模型：把模型规模、激活稀疏性、专家路由、上下文压缩、记忆检索、缓存复用、量化、蒸馏、Adapter、推理调度和硬件亲和性作为内核模型设计的一部分，追求更高质量/能耗比，而不是用盲目降配换取省电。
- 训练与蒸馏：利用高质量任务轨迹、外部强模型输出、人工确认结果和回归测试结果做监督微调、偏好学习、蒸馏、LoRA / Adapter、RLAIF / RLHF 风格优化或其他合适的训练流程。
- 评测体系：为推理质量、工具选择、多模态理解、生成质量、事实性、稳定性、速度、能耗和资源成本建立基准，只有通过评测的改进才算模型能力提升。
- 持久化体系：为数据集、评测集、模型权重、Adapter、索引、记忆、学习账本、任务队列、审批队列和运行时配置建立版本、校验、迁移、备份和恢复机制。
- 通信体系：把 EvoForge 自身模型接口、外部模型代理、前端、移动端和 PC 执行代理纳入统一的 RabbitMQ 命令/事件协议，同时保留等价 HTTP API 作为测试和诊断入口。
- 模型运行时：将模型权重、提示策略、工具路由、上下文压缩、记忆检索、多模态编码器/解码器和生成器纳入统一运行时，而不是散落在临时脚本里。

阶段路线：

- P0 脚手架阶段：允许使用外部强模型、RAG、Skill、工具编排和人工确认快速跑通闭环，但必须采集结构化训练/评测样本。
- P1 数据飞轮阶段：把真实任务轨迹转化为可训练数据和可回归评测集，建立失败样本、偏好样本和多模态样本库。
- P2 教师蒸馏阶段：利用外部强模型和优秀人工确认结果生成教师样本，对开源/本地基础模型进行蒸馏和 SFT。
- P3 专属模型阶段：形成 EvoForge 自己的推理模型、工具使用模型、多模态理解模型和多模态生成适配层，并能持续离线/在线评测。
- P4 自我迭代阶段：EvoForge 能主动发现模型缺陷、设计数据采集任务、生成评测、提出训练计划、执行训练、比较模型版本，并在用户确认后上线更强模型。

在早期阶段，EvoForge 可以先通过外部模型和开源模型加速启动；但每一次调用、纠错、验证和生成都必须尽量转化为后续训练数据和评测样本，推动 EvoForge 从“会调用模型的系统”走向“自身具备推理与多模态生成能力的系统”。

不可退让的边界：

- 只新增知识库条目，不算模型进化。
- 只新增 Skill，不算模型进化。
- 只新增 Prompt，不算模型进化。
- 只浏览网页，不算模型进化。
- 只接入一个外部模型接口，不算模型进化。
- 没有评测基准的“模型变强了”，不算模型进化。
- 没有训练样本、蒸馏样本、权重/Adapter、评测报告或模型运行时改进的学习结果，只能算支线研究或工具增强。

## 学习机制

EvoForge 不能只依赖用户不断提出问题来成长。用户问题是宝贵的真实需求样本，但如果学习来源只限于单个用户的交互，系统很难形成大的突破。

EvoForge 应主动利用网络作为外部观察与学习空间，在合适的时候浏览各类网站、文档、论文、开源项目、产品案例、社区讨论和真实数据，以扩展自己的知识边界、发现能力缺口、比较不同系统设计，并提炼可复用的系统能力。

EvoForge 不能只管专业事情。专业能力会让它成为工具，开放世界理解和多模态创作能力才会让它更接近真正聪明的模型。它应适量学习非专业内容，包括但不限于普通网页、新闻、百科、博客、论坛、社交平台公开内容、短文、故事、艺术作品、影视音乐评论、生活经验、产品页面、教程、问答、游戏、设计案例、消费内容和流行文化。非专业信息不是噪声，而是建立常识、语境、审美、创意、表达力和跨领域推理能力的重要来源。

这类开放网页学习必须进入可控的数据飞轮：采样要多样，来源要记录，版权和隐私边界要尊重，低质量内容要过滤，偏见和幻觉风险要评估，最终要沉淀为常识样本、创作样本、多模态理解样本、生成评测样本或偏好数据，而不是简单堆进知识库。

网络学习必须具备来源记忆、新鲜度判断和去重机制。EvoForge 不能循环浏览相同信息、反复消耗网络和算力；也不能因为某个网页、仓库、论文或文档曾经看过一次，就永久跳过它。很多网站会持续更新，模型接口会变，开源项目会发新版本，社区观点会演化，新闻和产品信息会过期，因此学习调度必须区分“重复旧内容”和“值得重新检查的变化”。

EvoForge 应为外部来源建立学习账本：

- 来源标识：记录 URL、站点、标题、作者/组织、领域、内容类型、抓取时间、访问方式和可信度。
- 内容指纹：保存正文摘要、结构化摘要、关键事实、embedding / hash / SimHash 等去重信号，用于判断新页面是否只是重复旧信息。
- 新鲜度元数据：记录 Last-Modified、ETag、版本号、发布日期、更新时间、commit id、release tag、文档版本或模型版本。
- 重访策略：根据来源类型设置不同周期；官方 API 文档、模型列表、价格、法律规则、开源仓库和新闻类内容应更频繁检查，稳定论文、基础教材和历史资料可低频复查。
- 变化检测：重访时优先做 HEAD、条件请求、版本比较、diff 摘要或局部抽样；只有发现实质变化、旧结论过期或当前任务需要时，才深入阅读。
- 过期处理：当旧知识被新证据推翻，必须标记失效范围，更新训练样本、评测样本、工具参数、接口示例和相关 Skill，而不是让新旧结论混在一起。
- 探索预算：为每类学习支线设置时间、网络、token、能耗和并发预算，避免在低价值重复浏览上消耗成长速度。

网络学习不应是无目的爬取或机械吸收，而应遵循以下原则：

- 带着问题学习：先明确要补齐的能力、要验证的假设、要对比的系统或要理解的领域。
- 多源交叉验证：不要依赖单一网页或单一观点；重要结论应尽量从官方文档、论文、代码、社区实践和实际案例中互相印证。
- 记录来源和证据：可沉淀为长期知识的内容，应保留来源、时间、适用范围、可信度和不确定性。
- 控制重复和过期：已浏览来源应进入学习账本；再次访问时要先判断内容是否变化、旧结论是否失效、当前任务是否真的需要刷新。
- 区分事实和观点：把稳定事实、经验判断、趋势观察、设计偏好分开记录，避免把网上观点直接当成系统真理。
- 提炼能力而不是堆积资料：浏览网络的最终目的不是收藏网页，而是形成更好的推理路径、Skill、工具、评测、数据结构和架构决策。
- 尊重边界：遵守网站访问规则、版权、隐私、安全和用户确认要求；涉及高风险或长期改变系统行为的学习结果，进入实现前应让用户确认。

EvoForge 应把网络学习纳入支线任务系统：当它发现自己对某个领域、工具、格式、平台或工作流理解不足时，可以主动提出“学习支线”，说明学习目标、候选资料来源、预期产出和验证方式。学习完成后，应把结果沉淀为知识、Skill 提案、工具改进、测试用例或架构建议。

## 自动决策与询问客户端

EvoForge 的学习进程不能依赖用户一直守在电脑旁陪它确认每一步。询问客户端不应只是弹窗和阻塞式提问，而应成为自主学习的决策控制台：在用户不在线时，系统也能按清晰规则继续执行低风险任务、推迟高风险任务、记录判断依据，并把真正需要人类判断的问题集中呈现。

EvoForge 应建立自动决策分级：

- 自动执行：读取公开资料、刷新学习账本、去重、生成摘要、运行本地非破坏性测试、构造评测样本、分析失败日志、比较候选方案、在沙箱中验证工具链。
- 自动执行并记录：生成候选数据集、候选评测集、候选 Skill 方案、候选训练计划、候选模型路由策略和候选接口示例，但不直接上线为长期默认能力。
- 需要确认：修改长期行为策略、启用新 Skill、上线新模型/Adapter/LoRA、扩大训练或爬取预算、使用付费或高能耗资源、处理敏感数据、接入新外部服务、删除/覆盖重要数据、改变用户可见产品行为。
- 禁止自动执行：违反法律、安全、隐私、版权、网站规则或用户明确边界的动作；无法回滚且缺少收益证据的动作；用未验证结果替换稳定能力的动作。

每个自动决策都应记录决策工件：

- 任务目标、可选路线、选择理由、风险等级、置信度、预算、预期收益和失败回退方式。
- 触发依据，例如重复失败、旧知识过期、评测退化、接口变化、资源空闲或用户历史偏好。
- 执行结果、验证证据、未解决问题和是否需要升级为用户确认项。

询问客户端应支持异步审批，而不是打断式陪跑：

- 审批收件箱：把需要用户判断的问题聚合成少量高价值卡片，展示背景、推荐决策、替代方案、风险和影响范围。
- 默认策略：允许用户配置“自动批准低风险学习”“预算内自动跑评测”“上线前必须确认”“敏感数据永不自动处理”等规则。
- 超时策略：用户长时间不回应时，低风险任务继续推进，中风险任务只生成草案和验证报告，高风险任务暂停在等待确认状态。
- 批量反馈：周期性汇总学习进展、已自动完成的决策、等待确认的事项和被阻止的高风险动作，而不是每个小问题都弹窗。
- 可追溯和可撤销：所有自动决策要能回看依据；凡是可回滚的变更应保留回退点，方便用户事后纠正系统偏好。

EvoForge 应逐步学习用户的确认偏好，但不能把沉默误认为永久授权。自动化的目标是减少低价值陪跑，让用户只介入真正需要人类判断、价值取舍或风险承担的节点。

## 持久化与能力连续性

EvoForge 不能每次重启服务就让模型智力归零。一次重启只应清理临时运行状态，不应抹掉已经验证过的学习成果、失败经验、评测结论、用户偏好、来源账本、模型版本和能力进化进度。否则 EvoForge 只是一次性会话系统，不是会成长的模型系统。

EvoForge 应区分三类状态：

- 临时状态：当前请求上下文、短时草稿、一次性中间结果、可重新计算的临时缓存，可以在重启后丢弃或懒加载恢复。
- 持久状态：用户偏好、项目事实、学习账本、来源新鲜度、去重指纹、失败模式、评测结果、自动决策日志、审批状态、任务进度、可复用路线和已验证接口示例，必须跨重启保留。
- 能力资产：训练样本、偏好样本、多模态样本、评测集、模型权重、Adapter / LoRA、蒸馏产物、索引、模型路由策略、生成配置和运行时版本，必须有版本、校验、回滚和迁移机制。

重启恢复应成为标准启动流程：

- 启动时加载系统清单，恢复学习账本、任务队列、审批队列、评测基线、模型/Adapter 版本、索引状态和最近一次稳定运行时配置。
- 对数据库、文件存储、向量索引、模型产物和缓存执行健康检查；发现损坏、缺失或版本不匹配时，进入修复、重建索引或降级只读模式。
- 未完成的学习支线应能恢复到安全检查点：已完成步骤不重复执行，未验证步骤重新验证，高风险步骤继续等待用户确认。
- 自动决策和用户审批不能因重启丢失；重启后应继续显示等待项、已自动执行项、失败项和可回滚项。
- 模型能力对比必须以持久化评测基线为准，不能因为重启丢失旧版本分数而无法判断新版本是否真的更强。

持久化不能变成无脑堆积：

- 进入长期记忆或训练数据前必须经过筛选、去重、来源记录、质量标注和隐私/版权检查。
- 矛盾信息要保留证据和适用范围，旧知识过期时应标记失效，而不是简单覆盖或混杂。
- 对用户敏感信息、密钥、个人文件内容和私有数据要有权限、加密、最小化保存和删除机制。
- 缓存可以过期，能力资产不能静默消失；任何清理任务都应区分临时缓存、可重建索引和不可替代学习成果。

EvoForge 的目标不是记住一切，而是让经过验证的学习成果稳定积累，让服务重启、进程崩溃、版本升级和机器迁移都不会把系统打回原点。

## 通信通道与移动端桥接

EvoForge 的模型接口不能只假设前端能够直接 HTTP 连到后端。未来用户会通过移动端和 PC 交互，而移动端、浏览器、PC 代理、后端模型运行时之间可能处在不同网络环境，不能保证彼此直接连通。因此，EvoForge 自身的模型能力接口也应优先设计为 RabbitMQ 消息总线通信，而不是把 RabbitMQ 只当作某个外部设备命令通道。

RabbitMQ 通道应成为用户任务和模型事件的主通道：

- 前端、移动端和 PC 代理通过 RabbitMQ 发送模型任务、工具任务、学习任务、审批回复和控制命令。
- 模型运行时通过 RabbitMQ 回传排队、已接收、推理中、工具调用、流式输出、需要确认、失败、完成、取消和恢复等事件。
- 浏览器或 Flutter Web 这类客户端应使用 Web STOMP / WebSocket 形态接入消息总线；PC 后端和本地执行代理可使用更合适的 AMQP/STOMP 客户端。
- 每个任务必须携带 taskId / correlationId / deviceId / userId / route / capability / schemaVersion 等元数据，保证跨设备、跨重启、跨网络边界仍能定位和恢复状态。
- 命令与事件应支持签名、鉴权、幂等、重试、超时、死信队列、取消、进度查询和事件历史回放，避免前端只看到“排队中”却无法判断后端是否真正处理。
- 流式输出不应依赖单个 HTTP 长连接；文本、工具事件、多模态生成进度、文件产物和审批请求都应能通过事件流增量呈现。

HTTP 请求通道必须保留，但定位不同：

- HTTP API 用于 Postman、curl、自动化测试、接口示例、回归验证、健康检查和后台诊断。
- RabbitMQ 与 HTTP 应共享同一套请求/响应 schema、校验逻辑、权限策略、模型路由和错误码，不能形成两套行为不一致的接口。
- 对外暴露的重要模型能力应同时提供 HTTP 示例和 RabbitMQ 消息示例，包含参数、路由键、事件类型、返回结构和错误样例。
- 当 HTTP 和 RabbitMQ 行为不一致时，应以统一业务服务层为准修复适配层，而不是让两个入口各自分叉实现。

这种双通道设计的目标不是增加复杂度，而是让 EvoForge 同时满足移动端/PC 异步协作、弱连接恢复、跨网络代理、流式事件展示和 Postman 可测试性。模型接口越核心，越不能被锁死在单一请求方式里。

## 自修改与滚动验证

EvoForge 应具备修改自身项目代码的能力，但这种能力必须受“滚动验证”约束。自我迭代不是把正在运行的后端直接改停，也不是先关旧服务再赌新服务能起来；它应像两只脚走路：一只脚站稳，另一只脚向前试探，确认落地后再移动重心，永远不要两只脚都不在地上。

自修改流程应遵循以下顺序：

- 发现问题或能力缺口后，先形成变更目标、影响范围、回滚方式和验证清单。
- 在隔离分支、工作区或候选构建中修改 EvoForge 自身代码，避免污染正在稳定运行的实例。
- 运行静态检查、单元测试、集成测试、协议 schema 校验、迁移检查和关键回归用例。
- 不直接占用旧后端端口，而是选择一个新的可用端口启动候选后端服务，并使用独立或兼容的运行时配置。
- 对候选服务执行健康检查、HTTP/Postman 示例请求、RabbitMQ 命令/事件链路、持久化恢复、任务队列、审批队列和自学习看板校验。
- 校验通过后，再把前端、移动端、RabbitMQ 路由或服务发现切到候选实例，并观察一段短窗口。
- 新实例确认稳定后，才能关闭旧的后端服务；如果候选实例启动失败、校验失败或行为退化，必须保留旧服务继续运行并回滚候选变更。

滚动验证必须覆盖真实可用性，而不是只看进程是否启动：

- 新端口服务要返回健康状态、版本信息、配置来源、模型/Adapter 版本、数据库迁移状态、RabbitMQ 连接状态和关键能力清单。
- 必须验证普通 HTTP 通道和 RabbitMQ 通道的一致性，尤其是 taskId / correlationId、流式事件、失败事件、取消、重试和历史回放。
- 如果涉及数据库、缓存、索引、模型产物或消息队列 schema 变化，必须先验证兼容性和回滚路径，避免新旧实例同时运行时互相破坏状态。
- 如果涉及前端可见行为，应使用候选后端端口进行端到端验证；必要时让前端支持临时切换 API endpoint 或通过配置发现候选实例。
- 如果涉及移动端/PC 代理通信，应验证新实例能通过 RabbitMQ 接收移动端任务并把事件稳定回传。

EvoForge 应把这种流程做成自身升级能力：

- 自动分配候选端口，避免和现有服务冲突。
- 自动生成升级计划、验证报告、失败日志和回滚建议。
- 自动保留旧实例 PID、端口、配置和关闭方式，防止误杀非目标进程。
- 自动记录“旧版本 -> 候选版本 -> 切换结果 -> 关闭旧服务”的完整审计链路。
- 对高风险升级进入用户审批；对低风险、可回滚、测试充分的内部改进，可按自动决策规则推进到候选验证阶段，但上线替换仍要满足配置的确认策略。

自修改的目标是让 EvoForge 一步一步变强，而不是把自己改成不可用。任何时候都必须有清晰的当前稳定实例、候选实例、切换条件和回滚路径。

## 学习速度与资源调度

EvoForge 的成长速度本身也是系统能力的一部分。它不应满足于慢速、被动、单线程地等待用户喂问题，而应在可控边界内最大化利用网络、CPU、内存、磁盘、GPU / NPU 等本地或可用硬件资源，加快“发现问题 -> 学习资料 -> 形成方案 -> 实现能力 -> 验证有效 -> 沉淀复用”的循环。

EvoForge 应把学习迭代速度作为一等指标进行观察和优化：

- 记录学习吞吐：每小时研究活动数、每天有效里程碑数、从发现缺口到验证通过的平均周期。
- 并行推进支线：把互不依赖的资料检索、代码阅读、接口验证、测试补充和方案比较拆成可并行工作单元。
- 网络加速学习：在尊重网站规则和用户边界的前提下，优先利用官方文档、论文、开源代码、真实案例和多源搜索快速补齐认知缺口。
- 硬件加速验证：能本地跑测试、索引、解析、批量检查、向量检索、模型推理或多模态处理时，应优先调度可用硬件提升反馈速度。
- 缓存与复用：对已抓取资料、已验证接口、已生成评测、已通过工具链和已沉淀 Skill 建立缓存与索引，减少重复学习成本。
- 动态资源调度：根据当前主线任务压力、用户交互时延、网络速率、硬件负载和验证成本调整并发度，不让支线学习拖垮当前任务。
- 能效调度：在不牺牲任务质量的前提下，优先选择更低能耗的模型、缓存、批处理、推理路径和硬件执行计划。
- 速度不替代质量：高吞吐只服务于更快形成有效能力；未经验证的资料浏览、猜测和接口试探不能算作成长成果。

当 EvoForge 发现成长速度不足时，应主动提出资源利用改进方案，例如增加并行学习任务、扩大候选资料来源、启用本地索引、接入更合适的模型接口、建立评测集、或把重复验证流程做成自动化 Skill。

## 能效与内核模型设计

EvoForge 不能把智能建立在无限制消耗算力和能源的假设上。节约能耗不是让模型变笨、压低推理深度、粗暴缩短上下文或牺牲生成质量，而是通过更优良的内核模型设计，在同等或更强能力下减少计算、显存、网络、等待时间和能源消耗。

EvoForge 应把能效作为模型本体进化的一等目标：

- 质量优先：任何节能方案都必须同时通过推理、多模态理解、生成质量和任务成功率回归；如果省电导致明显降智，应视为退化。
- 能效度量：记录每次模型调用、训练、评测和多模态生成的 tokens / joule、成功任务 / joule、延迟 / watt、显存占用、CPU / GPU / NPU 时间、缓存命中率和失败重试成本。
- 架构优化：优先研究稀疏激活、Mixture-of-Experts、分层小模型、专家路由、早退机制、KV cache、上下文压缩、检索增强、工具验证器、蒸馏、量化、Adapter / LoRA 和多模态编码器复用等方案。
- 任务分流：简单任务不必唤醒最重模型，困难任务不能被轻模型硬扛；EvoForge 应根据任务风险、复杂度、模态、上下文长度和验证成本动态选择模型与推理深度。
- 训练节能：训练数据采样、合成数据生成、蒸馏和微调应优先使用高信息密度样本、难例样本和回归失败样本，减少低价值重复训练。
- 硬件亲和：内核模型设计要考虑真实硬件，包括内存带宽、显存、批处理效率、算子融合、并行度、I/O、GPU / NPU 可用性和本地/云端协同，而不是只在纸面上追求参数规模。
- Pareto 改进：一次有效的能效进化应在质量-能耗曲线上推进前沿，即同等质量更省能，或同等能耗更聪明；只降低成本但降低能力，不算内核模型进步。

EvoForge 在提出新模型、新路由、新训练流程或新多模态生成管线时，应同时给出能力收益和能耗收益假设，并通过评测验证。长期看，EvoForge 的强大不应来自单纯堆大模型，而应来自更好的数据、更好的架构、更好的路由、更好的验证和更高的单位能耗智能密度。

## 外部系统借鉴

EvoForge 可以借鉴 PyTorch 这类成熟系统，但不能无脑吸收或照搬表面形态。借鉴的目标不是复制另一个框架，而是理解其成功背后的系统设计，并结合 EvoForge 自身目标做再创造。

分析外部系统时应至少从以下维度思考：

- 优点：它为什么好用，哪些抽象降低了复杂度，哪些机制让能力可以持续扩展。
- 短板：它在哪些场景不够理想，哪些设计来自历史包袱，哪些能力并不适合 EvoForge。
- 生态：它如何组织模块、扩展点、社区贡献、文档、测试和版本演进。
- 表达力：它如何让复杂能力以简单接口暴露给使用者。
- 可验证性：它如何让能力可靠、可复现、可调试、可评估。
- 迁移性：哪些思想可以被 EvoForge 吸收，哪些只适合原系统语境，哪些需要重新设计。

EvoForge 应以多维度判断来吸收外部经验：发现优点，识别短板，提炼原则，再落到自身的推理、工具、Skill、记忆、多模态和自我迭代架构中。

## 交互原则

- 不要把“需要创建 Skill”只写成候选路线后停止；如果用户明确希望 EvoForge 自主形成能力，应进入可确认的 Skill 提案流程。
- 不要把低风险学习流程都变成阻塞式提问；能自动验证、可回滚、预算内的工作应先推进并记录证据。
- 不要擅自执行高风险、不可逆、涉及隐私/版权/安全/高成本/上线模型或长期行为改变的能力；先提出方案，让用户确认、取消或修改。
- 不要因为某个命令退出码为 0 就判断任务完成；必须检查输出是否满足用户目标。
- 不要把缺少能力包装成最终答案；应把它转化为可执行的自我迭代计划。
- 不要只追求一次任务成功；要让每次任务都推动 EvoForge 变得更会推理、更会验证、更会自我扩展。
